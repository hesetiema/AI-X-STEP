下面直接给你一版 **“浏览器插件 + 页面轻桥接协议”方案的系统分层图 + 核心时序图（Mermaid 版）**。  
我按 MVP 视角来画，尽量突出：

- 低侵入
- 插件为主
- 页面轻桥接补语义
- 后端做诊断分析
- 工作台承接结果展示

我给你 4 张图：

1. 系统分层图  
2. 核心时序图：一次诊断会话  
3. 页面桥接与插件协同时序图  
4. 后端诊断分析链路图  

---

# 1. 系统分层图（Mermaid）

```mermaid
flowchart TB
  subgraph L1[浏览器侧 - 用户与页面]
    U1[User]
    P1[Business Page]
    P2[SPA Router / DOM / UI Components]
    P3[fetch / XHR / Browser Runtime]
  end

  subgraph L2[页面轻桥接层]
    B1[Global Context Bridge<br/>window.__DIAGNOSIS_CONTEXT__]
    B2[Custom Event Bridge<br/>diagnosis:interaction / diagnosis:state]
  end

  subgraph L3[浏览器插件层]
    E1[Plugin Panel<br/>开始/结束诊断]
    E2[Recorder Controller]
    E3[DOM / Interaction Observer]
    E4[Network Observer]
    E5[Error Observer]
    E6[Bridge Listener]
    E7[Session Builder]
    E8[Uploader]
  end

  subgraph L4[后端接入与标准化层]
    S1[Session Ingestion API]
    S2[Normalizer]
    S3[Sanitizer / Privacy Filter]
    S4[Evidence Builder]
  end

  subgraph L5[诊断分析层]
    D1[Diagnosis Session Store]
    D2[DiagnosisContext Builder]
    D3[Rule Engine]
    D4[Ranking Service]
    D5[Conclusion Service]
    D6[Domino Chain Builder]
    D7[Explanation Builder]
    D8[AI Enhancement Optional]
  end

  subgraph L6[工作台与查询层]
    W1[Diagnosis API]
    W2[Diagnosis Workstation]
    W3[Evidence Viewer]
    W4[Domino Chain View]
  end

  U1 --> E1
  U1 --> P1
  P1 --> P2
  P1 --> P3

  P1 --> B1
  P1 --> B2

  P2 --> E3
  P3 --> E4
  P3 --> E5
  B1 --> E6
  B2 --> E6

  E1 --> E2
  E3 --> E7
  E4 --> E7
  E5 --> E7
  E6 --> E7
  E2 --> E7
  E7 --> E8

  E8 --> S1
  S1 --> S2
  S2 --> S3
  S3 --> S4

  S1 --> D1
  S4 --> D2
  D1 --> D2
  D2 --> D3
  D3 --> D4
  D4 --> D5
  D5 --> D6
  D5 --> D7
  D6 --> D7
  D7 --> D8

  D5 --> W1
  D6 --> W1
  D7 --> W1
  D8 --> W1

  W1 --> W2
  W2 --> W3
  W2 --> W4
```

---

# 2. 核心时序图：一次诊断会话（Mermaid）

这张图描述：

- 用户手动开始诊断
- 插件采集页面行为、请求、错误
- 页面通过轻桥接补充业务语义
- 插件结束录制并提交
- 后端分析并返回 diagnosis result

```mermaid
sequenceDiagram
  autonumber
  participant U as User
  participant PAGE as Business Page
  participant BRIDGE as Page Bridge
  participant PLUGIN as Browser Extension
  participant API as Session Ingestion API
  participant STORE as Session Store
  participant CTX as DiagnosisContext Builder
  participant RULE as Rule Engine
  participant RANK as Ranking
  participant CONC as Conclusion
  participant DOM as Domino Chain Builder
  participant EXP as Explanation Builder
  participant UI as Diagnosis Workstation

  U->>PLUGIN: 点击“开始诊断”
  PLUGIN->>PLUGIN: 创建 diagnosis session
  PLUGIN->>PAGE: 开始监听页面事件/请求/错误

  U->>PAGE: 复现问题
  PAGE-->>PLUGIN: click / submit / route change
  PAGE-->>PLUGIN: fetch / xhr request + response
  PAGE-->>PLUGIN: js error / promise rejection

  PAGE->>BRIDGE: 暴露 context / dispatch interaction,state events
  BRIDGE-->>PLUGIN: bridge.context / bridge.interaction / bridge.state

  U->>PLUGIN: 点击“结束并提交”
  PLUGIN->>PLUGIN: 组装 DiagnosisSession
  PLUGIN->>API: POST /diagnosis-sessions
  API->>STORE: 保存 session
  STORE-->>API: sessionId

  API->>CTX: 构建 DiagnosisContext
  CTX-->>RULE: evidence set
  RULE-->>RANK: findings
  RANK-->>CONC: ranked findings
  CONC-->>DOM: top/supporting/symptoms
  CONC-->>EXP: structured result
  DOM-->>EXP: domino chain
  EXP-->>API: diagnosis result

  API-->>PLUGIN: diagnosisId
  PLUGIN-->>U: 提交成功，可查看结果

  U->>UI: 打开诊断详情
  UI->>API: GET /api/v1/diagnosis/:id
  API-->>UI: diagnosis result + domino chain + evidence summary
```

---

# 3. 页面桥接与插件协同时序图（Mermaid）

这张图更强调你关心的“插件 + 轻桥接协议”协作方式。

```mermaid
sequenceDiagram
  autonumber
  participant PAGE as Business Page
  participant APP as App Business Logic
  participant BRIDGE as Light Bridge Layer
  participant EXT as Browser Extension
  participant REC as Session Recorder

  EXT->>PAGE: 注入 content script / attach listeners
  EXT->>REC: recorder ready

  APP->>BRIDGE: set window.__DIAGNOSIS_CONTEXT__
  BRIDGE-->>EXT: readable global context

  APP->>BRIDGE: dispatch diagnosis:interaction
  BRIDGE-->>EXT: CustomEvent(detail={businessAction,targetId})

  APP->>BRIDGE: dispatch diagnosis:state
  BRIDGE-->>EXT: CustomEvent(detail={type,componentName})

  EXT->>REC: append bridge event
  PAGE-->>EXT: DOM interaction / network / error
  EXT->>REC: append observed event

  REC->>REC: merge timeline by timestamp
  REC->>REC: bind bridge semantic hints to observed events
```

---

# 4. 后端诊断分析链路图（Mermaid）

这张图可以直接放到 Diagnosis 后端架构章节，强调插件 session 进入后端后的分析流程。

```mermaid
flowchart LR
  A[Diagnosis Session Upload] --> B[Session Store]
  A --> C[Normalizer]
  C --> D[Privacy Filter]
  D --> E[Evidence Builder]
  B --> F[DiagnosisContext Builder]
  E --> F

  F --> G[Observation Timeline]
  F --> H[Bridge Semantic Hints]
  F --> I[Request / Error / UI State Correlation]

  G --> J[Rule Engine]
  H --> J
  I --> J

  J --> K[Candidate Findings]
  K --> L[Ranking Service]
  L --> M[Conclusion Service]

  M --> N1[Top Cause]
  M --> N2[Supporting Causes]
  M --> N3[Symptoms]

  N1 --> O[Domino Chain Builder]
  N2 --> O
  N3 --> O

  M --> P[Explanation Builder]
  O --> P
  P --> Q[AI Enhancement Optional]
  Q --> R[Diagnosis Result]
```

---

# 5. 可直接配在文档里的说明文字

下面这段你可以直接放在图下面。

---

## 5.1 系统分层图说明
该方案采用“浏览器插件主采集 + 页面轻桥接补语义 + 后端诊断服务分析”的分层架构。浏览器插件负责从页面外部观察交互、请求、异常等事实信号，页面轻桥接层以全局上下文对象和自定义事件形式补充必要业务语义，后端对诊断会话进行标准化、证据构建、规则分析、归因总结和 domino chain 生成，最终由工作台提供诊断结果展示和证据查看能力。

## 5.2 核心时序图说明
一次诊断以用户手动开始录制为起点，插件在会话期间记录页面交互、网络请求、异常和桥接事件。结束录制后，插件将完整 DiagnosisSession 上传到后端，后端基于统一 Evidence 模型构建 DiagnosisContext，并按“规则召回 → 排序 → 归因总结 → domino chain → explanation”的链路产出诊断结果。该流程避免了对业务系统的重度侵入，适合 MVP 阶段快速验证。

## 5.3 桥接协同说明
页面轻桥接协议负责补充插件难以稳定获取的业务语义，例如当前模块、businessAction 和关键业务状态。插件作为外部观察者，一方面收集黑盒可见事实，另一方面监听桥接层发出的上下文与语义事件，并在会话构建阶段将两类信息按时间轴合并，从而提升后端根因分析的可解释性和准确性。

## 5.4 后端分析链路说明
后端不直接依赖页面内部实现细节，而是以插件上传的 session 为统一输入，经标准化和脱敏后构造成 Evidence 集合，再结合桥接语义提示与请求/错误/UI 状态关联关系进行规则命中与排序。最终输出 top cause、supporting causes、symptoms 和 domino chain，使诊断结果既具可计算性，也具可展示性。

---

# 6. 我建议你文档里的摆放顺序

如果这是正式方案文档中的一个章节，建议顺序：

1. **系统分层图**
2. **核心时序图：一次诊断会话**
3. **页面桥接与插件协同时序图**
4. **后端诊断分析链路图**

这样从整体到流程再到细部，最顺。

如果你愿意，我下一条可以继续补：

**“浏览器插件 + 页面轻桥接协议”方案的数据模型设计图（Mermaid erDiagram / classDiagram 版）**。